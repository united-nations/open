/** Hardcoded product flags. Flip to `true` to restore the hidden surfaces. */
export const SHOW_TRUST_FUNDS = true;
export const SHOW_FIELD_MISSIONS = false;

/** Optional sorting controls in the Locations and Goals UNINFO sections. */
export const SHOW_UNINFO_SORT_CONTROLS = false;
